
# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# Allure
/allure-results/
/allure-report/
/.allure/

# Playwright MCP scratch (browser snapshots, console logs, seed test)
/.playwright-mcp/
/tests/seed.spec.ts

# playwright-bdd compiles .feature → .spec.js here at runtime
/.features-gen/

# Run history (per-run + per-test summaries that accumulate machine-locally —
# used by the status-bar sparkline, ETA estimator, and flakiness detector.
# Shouldn't be checked in: it's noisy, would conflict between teammates, and
# is regenerated automatically as runs happen.)
/reports/history.jsonl
/reports/test-history.jsonl
